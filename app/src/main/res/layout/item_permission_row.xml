<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View"/>
        <variable
            name="permission"
            type="com.persy.learnandroid.model.Permission" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="16dp"
        android:layout_marginVertical="5dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="2dp"
        app:cardBackgroundColor="@color/white">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="14dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/tvPermissionName"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginEnd="8dp"
                    android:text="@{permission.name != null ? permission.name : `Không tên`}"
                    android:textColor="@color/text_primary"
                    android:textSize="15sp"
                    android:textStyle="bold"
                    tools:text="Create admin account" />

                <TextView
                    android:id="@+id/tvPcode"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:background="@drawable/bg_badge_pcode"
                    android:fontFamily="monospace"
                    android:text="@{permission.pcode}"
                    android:textColor="@color/badge_pcode_text"
                    android:textSize="11sp"
                    android:textStyle="bold"
                    android:visibility="@{permission.pcode != null &amp;&amp; !permission.pcode.trim().isEmpty() ? View.VISIBLE : View.GONE}"
                    tools:text="ACC_C_AD" />
            </LinearLayout>

            <TextView
                android:id="@+id/tvDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:text="@{permission.description.trim()}"
                android:textColor="@color/text_secondary"
                android:textSize="13sp"
                android:visibility="@{permission.description != null &amp;&amp; !permission.description.trim().isEmpty() ? View.VISIBLE : View.GONE}"
                tools:text="Create an admin account" />

        </LinearLayout>

    </androidx.cardview.widget.CardView>
</layout>

