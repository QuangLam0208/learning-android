<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="#F0F4F8"
    android:paddingHorizontal="16dp"
    android:paddingVertical="10dp"
    android:layout_marginTop="12dp"
    android:layout_marginBottom="4dp">

    <ImageView
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:src="@drawable/ic_folder_group"
        android:contentDescription="Group icon" />

    <TextView
        android:id="@+id/tvGroupName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="8dp"
        android:textColor="#1E293B"
        android:textSize="14sp"
        android:textStyle="bold"
        tools:text="Nhóm: Account" />

    <TextView
        android:id="@+id/tvGroupCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_badge_pcode"
        android:textColor="@color/badge_pcode_text"
        android:textSize="12sp"
        android:textStyle="bold"
        tools:text="7 quyền" />

</LinearLayout>
